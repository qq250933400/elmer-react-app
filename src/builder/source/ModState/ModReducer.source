const InitializeState = {
    myReducer: 'ModIndex'
};
export default (state, action) => {
    const resultState = {
        ...InitializeState,
        ...state
    };
    switch (action.type) {
        default:
            return resultState;
    }
};
