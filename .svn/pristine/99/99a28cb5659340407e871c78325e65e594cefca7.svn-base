import { connect } from 'react-redux';
import { mapStateToProps, mapDispatchToProps } from './mapProperties';
import { PageContainer } from '../common/index';

const container = connect(
    mapStateToProps,
    mapDispatchToProps
)(PageContainer);

export default container;
